<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:id="@+id/menuNonSearchGroup">
        <item
            android:visible="false"
            android:id="@+id/menuItemLocate"
            android:icon="@drawable/ic_my_location_24dp"
            android:title="@string/menu_item_label_locate"
            app:showAsAction="ifRoom" />
        <item
            android:id="@+id/menuItemRefresh"
            android:icon="@drawable/ic_refresh_24dp"
            android:title="@string/menu_item_label_refresh"
            android:visible="false"
            app:showAsAction="ifRoom" />
    </group>
    <item
        android:id="@+id/menuItemSearch"
        android:icon="@drawable/ic_search_24dp"
        android:title="@string/menu_item_label_search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView" />
</menu>